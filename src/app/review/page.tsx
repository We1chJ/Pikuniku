"use client";

import StudySession from "@/components/StudySession";

/** Material coming back round. Never capped — it's work already owed. */
export default function Review() {
  return <StudySession mode="reviews" />;
}
